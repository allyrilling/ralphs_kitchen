{
  "name": "ralphs-kitchen",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"hugo server\"",
    "build": "tinacms build && hugo --minify"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.6.2"
  }
}
